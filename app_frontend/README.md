# Shoe Cart Frontend

Quick React (Vite) frontend that integrates with the existing backend API at `http://localhost:9000`.

Run locally:

```bash
cd "app_frontend"
npm install
npm run dev
```

The dev server runs on port 3000 and proxies `/v1/api` to `http://localhost:9000`.
